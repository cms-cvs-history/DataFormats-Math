process read = {
  source = PoolSource {
    untracked int32 maxEvents = 10
    untracked vstring fileNames = { "file:math.root" }
  }
  
  module rm = ReadMath { 
    InputTag src = wm
  }
  
  path p = { rm }
}
