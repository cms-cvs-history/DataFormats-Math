process PROD = {
  source = EmptySource {
    untracked int32 maxEvents = 10
  }
  
  module wm = WriteMath { }
  
  module out = PoolOutputModule {
    untracked string fileName = "math.root"
  }

  path p = { wm }
  endpath ep = { out }
}
